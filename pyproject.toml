[project]
name = "modal-gharchive-dwh"
version = "0.1.0"
description = "Modal app for querying the GH Archive"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "duckdb>=1.3.2",
    "modal>=1.1.0",
    "requests>=2.32.5",
]
